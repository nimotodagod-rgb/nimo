services:
  - type: web
    name: editor-conquistando
    runtime: docker
    dockerfilePath: ./web/Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /health
    envVars:
      - key: APP_PIN
        sync: false
      - key: PORT
        value: "10000"
